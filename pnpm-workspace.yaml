packages:
  - "apps/*"
  - "packages/*"
  # Example plugins are real, independent plugin projects. In the workspace so
  # they resolve @starbase/plugin-sdk and so CI compiles them — an example that
  # no longer builds is worse than no example.
  - "plugins/examples/*"

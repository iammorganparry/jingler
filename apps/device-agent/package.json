{
  "name": "@jingler/device-agent",
  "version": "2.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node22 --outfile=dist/jingler-device.mjs --banner:js='#!/usr/bin/env node\nimport { createRequire as __jinglerCreateRequire } from \"node:module\"; const require = __jinglerCreateRequire(import.meta.url);'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@effect/platform-node": "^0.107.0",
    "@jingler/cli-adapters": "workspace:*",
    "@jingler/core": "workspace:*",
    "effect": "^3.21.4",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@jingler/tsconfig": "workspace:*",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.0",
    "rimraf": "^6.0.1",
    "vitest": "^3.2.6"
  }
}
